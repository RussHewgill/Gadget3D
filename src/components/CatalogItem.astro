---
import { Image } from 'astro:assets';
const { item } = Astro.props;
// const { has_image } = Astro.props;
// const { image_url } = Astro.props;
---

<div class="catalog-item">
  <!-- {has_image && <Image
    src={import(`../assets/images/things/${category}.jpg`)}
    height={300}
    alt="test alt"
    />
  } -->
  <!-- {has_image && <Image
    src={import(`../assets/images/things/${}.jpg`)}
    height={300}
    alt="test alt"
    />
  } -->
  {item.item_name}
  <p>${item.price}</p>
</div>
