---
import BaseLayout from '../layouts/BaseLayout.astro';
import FrontPageCard from '../components/FrontPageCard.astro';

// const uniqueTags = [...new Set(data.map((t) => t.tags).flat())];

// import {fetchSquareCatalog} from '../scripts/square.ts';
// import test_catalog from '../assets/sample_catalog.json';

// const catalog = await fetchSquareCatalog();
// // const catalog = await fetchSquareCatalogTest(test_catalog);
// // console.log("catalog = ", catalog);

// console.log("item = ", catalog.items[0]);

// catalog.items.forEach((item) => {
//   console.log("item = ", item);
// });

// import { Image } from 'astro:assets';
// import img from "../assets/images/things/adult_gemstone.jpg";

---

<BaseLayout pageTitle="Gadget3D">

  <div class="category-grid grid gap-2.5 lg:grid-cols-3 py-4">

    <FrontPageCard title="Dragons" 
      image={import(`../assets/images/things/adult_gemstone.jpg`)}/>
    <FrontPageCard title="Baby Dragons" 
      image={import(`../assets/images/things/baby_crystalwing.jpg`)}
      link="/things/Baby Dragon"/>
    <FrontPageCard title="Fidgets" 
      image={import(`../assets/images/categories/Fidgets.jpg`)}/>
    <FrontPageCard title="Critters" 
      image={import(`../assets/images/things/critters1.jpg`)}
      link="/things/Critters"/>
    <FrontPageCard title="Creepy Crawlies" 
      image={import(`../assets/images/things/tiny_spider.jpg`)}/>
    <FrontPageCard title="Dice Towers" 
      image={import(`../assets/images/things/crystal_mushroom_dicetower.jpg`)}/>
      
    <!-- <FrontPageCard title="Dinosaurs" 
        image={import(`../assets/images/things/ferret.jpg`)}/> -->
    <!-- <FrontPageCard title="Frogs" 
        image={import(`../assets/images/things/ferret.jpg`)}/> -->
    <!-- <FrontPageCard title="Turtles" 
        image={import(`../assets/images/things/ferret.jpg`)}/> -->
    <!-- <FrontPageCard title="Keychains and Bag Charms" 
        image={import(`../assets/images/things/ferret.jpg`)}/> -->
  </div>

  <!-- <div>
    <div>
      <h2 class="font-bold text-center">Artists</h2>
      <div class="category-grid grid gap-2.5 lg:grid-cols-3">
        {data.filter((item) => item.is_artist).map((item) => (
          <CategoryItem item={item}/>
          ))}
    </div>
  </div> -->
    
  <!-- <h2>Categories</h2> -->
  <!-- <div>
    <div class="category-grid grid gap-2.5 lg:grid-cols-3">
      {data.filter((item) => !item.is_artist).map((item) => (
        <CategoryItem item={item}/>
        ))}
    </div>
  </div> -->

  <!-- </div> -->

</BaseLayout>
