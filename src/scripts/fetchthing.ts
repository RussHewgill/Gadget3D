// import { supabase } from '../supabase';

type ThingProps = {
  name: string,
  variation: string,
  category: string,
  price: number,
  // tags: {tag: string}[]
}

// export const ThingProps = ({name, variation, category, price}: ThingProps) => {
//   const { data, error } = await supabase
//     .from('things')
//     .select();
  
// }

// console.error("wat");

// const { data, error } = await supabase
//     .from('things')
//     .select();

// if (error) {
//   console.error(error)
// }


